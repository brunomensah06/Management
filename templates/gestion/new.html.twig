{% extends 'base.html.twig' %}

{% block title %} Nouveau projet{% endblock %}

{% block body %}
<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a href="{{ path('home')}}" class="navbar-brand" href="#">SAFEC Management</a>
  

  </nav>
<div class="container">
  <div class="mt-5 bg-warning"></div>

    <h1>Faire un nouveau enregistrement</h1>

    {{ include('gestion/_form.html.twig') }}

    <a href="{{ path('gestion_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
